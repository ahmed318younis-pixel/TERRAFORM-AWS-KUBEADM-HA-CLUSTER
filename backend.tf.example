terraform {
  backend "s3" {
    # Configure these values through -backend-config or replace them here.
    # bucket       = "my-terraform-state-bucket"
    # key          = "kubeadm-ha/terraform.tfstate"
    # region       = "us-east-1"
    # encrypt      = true
    # use_lockfile = true
  }
}
